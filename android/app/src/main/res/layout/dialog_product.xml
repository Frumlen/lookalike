<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingHorizontal="20dp"
    android:paddingTop="8dp"
    android:paddingBottom="4dp">

    <TextView style="@style/FieldLabel" android:text="@string/field_name" />
    <EditText
        android:id="@+id/name"
        style="@style/Field"
        android:inputType="textCapSentences"
        android:hint="@string/field_name_hint" />

    <TextView style="@style/FieldLabel" android:text="@string/field_plu" />
    <EditText
        android:id="@+id/plu"
        style="@style/Field"
        android:inputType="number"
        android:hint="@string/field_plu_hint" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingTop="6dp"
        android:textSize="12sp"
        android:textColor="@color/muted"
        android:text="@string/field_plu_note" />

    <TextView style="@style/FieldLabel" android:text="@string/field_barcode" />
    <EditText
        android:id="@+id/barcode"
        style="@style/Field"
        android:inputType="number"
        android:hint="@string/field_barcode_hint" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingTop="6dp"
        android:paddingBottom="8dp"
        android:textSize="12sp"
        android:textColor="@color/muted"
        android:text="@string/field_barcode_note" />
</LinearLayout>
